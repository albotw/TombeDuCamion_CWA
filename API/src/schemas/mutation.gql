type Mutation {
    "PRODUITS"
    createProduct(
        auth: AuthInfo,
        seller: String, 
        title: String, 
        stock: Int, 
        description: String, 
        category: String, 
        price: Float,   
    ) : String "retourne p_uid pour l'upload d'images."

    updateProduct(
        auth: AuthInfo,
        p_uid: String,
        title: String,
        stock: String,
        description: String,
        category: String,
        price: Float,
    ) : String

    processOrder(
        auth: AuthInfo,
        items: [Order]
    ) : String

    "COMMENTAIRES"
    createComment(
        auth: AuthInfo,
        p_uid: String,
        message: String,
        note: Float
    ) : String

    updateComment(
        auth: AuthInfo,
        c_uid: String,
        message: String,
        note: String,
    ) : String

    deleteComment(
        auth: AuthInfo,
        p_uid: String,
        c_uid: String,
    ): Boolean

    "UTILISATEURS"
    createUser(
        nickname: String,
        email: String,
        password: String
    ) : String

    updateUser(
        auth: AuthInfo,
        email: String,
        nickname: String,
    ) : String
}

input Order {
    p_uid: String!,
    quantity: Int!
}